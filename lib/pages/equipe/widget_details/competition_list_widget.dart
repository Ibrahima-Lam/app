import 'package:flutter/material.dart';

class CompetitionListWidget extends StatelessWidget {
  const CompetitionListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Center(
        child: Text('Les competitions'),
      ),
    );
  }
}
