enum PlatformType {
  mobile,
  web,
  desktop,
}

// This enum represents the different platform types that the application can run on.
// It can be used to conditionally render UI or execute platform-specific logic.
